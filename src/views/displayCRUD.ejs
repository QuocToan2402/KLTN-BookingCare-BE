<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta http-equiv="X-UA-Compatible" content="IE=edge" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<title>Get CRUD</title>
		<style>
			table {
				font-family: arial, sans-serif;
				border-collapse: collapse;
				width: 100%;
			}

			td,
			th {
				border: 1px solid #dddddd;
				text-align: left;
				padding: 8px;
			}

			tr:nth-child(even) {
				background-color: #dddddd;
			}
			.btn-edit {
				color: #0818a8;
				font-weight: 500;
				border-radius: 3px;
				border: none;
			}
			.btn-delete {
				color: #e71010;
				font-weight: 500;
				border-radius: 3px;
				border: none;
			}
		</style>
	</head>
	<body>
		<table>
			<tr>
				<th>ID</th>
				<th>EMAIL</th>
				<th>USERNAME</th>
				<th>FIRSTNAME</th>
				<th>LASTNAME</th>
				<th>ACTION</th>
			</tr>
			<% for(var i=0; i < dataTable.length; i++) { %>
			<tr>
				<td><%= dataTable[i].id %></td>
				<td><%= dataTable[i].email %></td>
				<td><%= dataTable[i].username %></td>
				<td><%= dataTable[i].firstName %></td>
				<td><%= dataTable[i].lastName %></td>
				<td>
					<button
						onclick="location.href='/edit-crud?id=<%= dataTable[i].id %>'"
						class="btn-edit"
						type="button"
					>
						EDIT
					</button>
					<button
						onclick="location.href='/delete-user?id=<%= dataTable[i].id %>'"
						class="btn-delete"
						type="button"
					>
						DELETE
					</button>
				</td>
			</tr>
			<% } %>
		</table>
	</body>
</html>
